# ── 빌드 아웃풋 ───────────────────────────────────────
dist/

# ── 의존성 ────────────────────────────────────────────
node_modules/

# ── 환경변수 (DB 비밀번호, JWT 시크릿 등 절대 커밋 금지) ──
.env
.env.*
!.env.example
.env.local
.env.*.local

# ── 로그 ─────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── TypeORM 마이그레이션 자동생성 임시파일 ────────────
# src/migrations/*.js  # 컴파일된 JS는 제외, TS만 커밋

# ── 타입스크립트 빌드 ─────────────────────────────────
*.tsbuildinfo

# ── OS / 에디터 ───────────────────────────────────────
.DS_Store
Thumbs.db
.fleet
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln

# ── 업로드 파일 (로컬 저장소 사용 시) ────────────────
uploads/
public/uploads/
